# To-Do List CLI (C++)

A simple command-line To-Do List application written in C++. 
Users can add tasks, view tasks, delete tasks, and save tasks to a file.

## Features
- Add new tasks
- View all tasks
- Delete tasks
- Save tasks to a file
- Load tasks on startup
